<Window x:Class="Bulico.MullionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="一键横挺"
        Height="280" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        SizeToContent="Height">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="请输入偏移值（mm）：" FontSize="14" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="OffsetInput" Height="30" FontSize="16"
                 VerticalContentAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="多个值用空格分隔，如：500 600" FontSize="11"
                   Foreground="#888" Margin="0,4,0,2"/>
        <TextBlock Grid.Row="3" Text="支持批量公式 n*间隔，如：3*600" FontSize="11"
                   Foreground="#888" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,12">
            <RadioButton x:Name="FromBottom" Content="自底部" IsChecked="True"
                         Margin="0,0,24,0" FontSize="13"/>
            <RadioButton x:Name="FromTop" Content="自顶部" FontSize="13"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="确定" Width="80" Height="30"
                    Margin="0,0,8,0" Click="OkButton_Click" IsDefault="True"/>
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="30"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
